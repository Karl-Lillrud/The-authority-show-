import React from 'react';
import { Box } from '@mui/material';
import TranslationManager from '../components/TranslationManager';

const TranslationManagerRoute = () => {
  return (
    <Box>
      <TranslationManager />
    </Box>
  );
};

export default TranslationManagerRoute; 